---
title: "Seeing is Understanding: Unlocking Causal Attention into Modality-Mutual Attention for Multimodal LLMs"
title_zh: 见即理解：将因果注意力转化为模态互注意力用于多模态大语言模型
authors: "Wei-Yao Wang, Zhao Wang, Helen Suzuki, Yoshiyuki Kobayashi"
date: 2026-04-30
pdf: "https://openreview.net/pdf/7d01cf9228d5b8cadf081066c792c7d2b57177ff.pdf"
tags: ["query:vlam"]
score: 7.0
evidence: 多模态注意力机制改进跨模态融合
tldr: 该论文针对多模态大语言模型中视觉-语言不对齐问题，提出将因果注意力扩展为模态互注意力，从架构层面增强跨模态融合。实验证明该方法在多种视觉-语言基准上提升了响应与输入的对齐度，为多模态融合架构提供了新思路。
source: ICML-2026-Accepted
selection_source: conference_retrieval
motivation: 现有MLLM存在视觉-语言不对齐，导致生成文本与图像输入不一致。
method: 提出模态互注意力机制，替换传统因果注意力以增强视觉与语言特征的交互。
result: 在多个基准上显著提升了视觉-语言对齐性能。
conclusion: 从架构层面改进注意力可有效缓解多模态不对齐问题。
---

## Abstract
Recent Multimodal Large Language Models (MLLMs) have demonstrated significant progress in perceiving and reasoning over multimodal inquiries, ushering in a new research era for foundation models. However, vision-language misalignment in MLLMs has emerged as a critical challenge, where the textual responses generated by these models are not factually aligned with the given text-image inputs. Existing efforts to address vision-language misalignment have focused on developing specialized vision-language connectors or leveraging visual instruction tuning from diverse domains. In this paper, we tackle this issue from a fundamental yet unexplored perspective by revisiting the core architecture of MLLMs. Most MLLMs are typically built on decoder-only LLMs consisting of a causal attention mechanism, which *limits the ability of the earlier modalities (e.g., images) to incorporate information from the latter modalities (e.g., text)*. To address this problem a MLLM that unlocks causal attention into our proposed modality-mutual attention (MMA) to enable image tokens to attend to text tokens. This simple yet effective design allows MMA to achieve state-of-the-art performance in 12 multimodal understanding benchmarks (**+6.2\% on average across 3 LLMs backbones**) without introducing additional parameters. Our MMA design is intended to be generic, allowing for applications across various modalities, and scalable to accommodate diverse multimodal scenarios.

---

## 论文详细总结（自动生成）

# 论文总结：《Seeing is Understanding: Unlocking Causal Attention into Modality-Mutual Attention for Multimodal LLMs》

## 1. 核心问题与整体含义（研究动机和背景）
- 多模态大语言模型（MLLMs）在感知和推理多模态查询方面取得了显著进展，但**视觉-语言不对齐**问题成为关键挑战：模型生成的文本响应未能与给定的文本-图像输入事实性对齐。
- 现有工作主要从两个方向尝试解决：开发专门的视觉-语言连接器（vision-language connector）或利用多样化领域的视觉指令微调（visual instruction tuning）。本文则从**更根本但未被探索的角度**出发，重新审视MLLM的核心架构。
- 大多数MLLM基于仅解码器（decoder-only）的LLM构建，其内部采用**因果注意力（causal attention）机制**，该机制限制了**先出现模态（如图像）** 获取**后出现模态（如文本）** 信息的能力——即图像tokens无法关注文本tokens，导致跨模态信息融合受限。

## 2. 方法论：核心思想与关键技术细节
- **核心思想**：将传统因果注意力解锁为**模态互注意力（Modality-Mutual Attention, MMA）**，使图像tokens能够关注文本tokens，从而增强视觉与语言特征的交互。
- **关键技术细节**：
  - MMA设计不引入额外参数，直接修改注意力计算方式，允许跨模态的**双向信息流**（但限于模态顺序上提前的模态关注后续模态，而非完全双向？文中表述“image tokens to attend to text tokens”暗示仅解决单向限制）。
  - 方法具有**通用性**：可应用于多种模态（如图像、文本、音频等），并**可扩展**至不同多模态场景。
  - 由于不增加参数，该设计可轻松集成到现有decoder-only架构中，无需改变模型容量。
- **公式/算法流程**：论文摘要未给出具体公式，仅文字说明。可理解为在注意力计算中，对因果mask进行修改或替换，使得图像token在自注意力过程中能够看到文本token的表示（而不仅仅是之前位置的token）。

## 3. 实验设计
- **使用的数据集/基准**：在**12个多模态理解基准**上评估，但摘要未列出具体数据集名称（如MMBench、MME、SEED-Bench等）。
- **对比的方法**：未提及具体对比基线（如LLaVA、Qwen-VL、InstructBLIP等），仅说明在**3个不同的LLM骨干网络**上应用MMA后，平均提升**+6.2%**。
- **评估指标**：未具体说明，但推断为各基准的标准准确率或得分。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量、训练时长等算力细节。摘要仅提及“without introducing additional parameters”，但未提及训练或微调所需的硬件资源。

## 5. 实验数量与充分性
- **实验数量**：至少包含12个多模态理解基准上的结果，以及3种LLM骨干的对比。文中未提及其他消融实验（如对MMA不同变体的消融、不同模态顺序的测试等）。
- **充分性评价**：由于摘要信息有限，难以判断实验是否充分。但12个基准和3种骨干的覆盖范围较为广泛，具备一定说服力。然而，是否进行了**与现有SOTA方法的公平对比**（如相同训练数据、相同基座模型）未知；**缺乏消融实验**细节（例如对比仅替换部分注意力层、或保留因果注意力的基线）；**未提及零样本/少样本设定**，仅笼统说“achieve state-of-the-art”。因此实验设计可能存在不足。

## 6. 主要结论与发现
- 提出的MMA机制在12个多模态理解基准上达到**最先进性能**，在3种不同LLM骨干上平均提升**+6.2%**。
- 该设计**简洁有效**，**不引入额外参数**，可直接替换现有MLLM中的因果注意力模块。
- 验证了从**架构层面改进注意力**可以有效缓解多模态不对齐问题，为多模态融合提供了新思路。

## 7. 优点
- **创新性**：从最基础的注意力机制入手，而非添加额外模块或数据，视角独特。
- **高效性**：不增加参数，计算开销几乎不变，易于部署和迁移。
- **通用性**：设计适用于多种模态和多种LLM骨干，具备很强的可扩展性。
- **实证提升显著**：平均+6.2%的增益在12个基准上具有统计意义。

## 8. 不足与局限
- **信息缺失**：摘要未提供详细的实验设置、数据集列表、对比基线、超参数等，难以复现和验证结论的可靠性。
- **未解决完全双向？**：MMA仅允许图像关注文本，但文本是否也能以相同方式关注图像？若文本仍保持因果顺序，则仍存在不平衡。论文未澄清这一点。
- **潜在偏差风险**：仅在理解类基准上评估，未提及其他任务（如生成式对话、视觉定位、文本到图像生成等），可能掩盖方法在生成质量或多样性上的折衷。
- **可解释性不足**：未从理论或可视化角度解释为什么简单的注意力修改能解决不对齐问题。
- **算力资源未报告**：缺乏可复现性所需的计算成本信息。

（完）
